- provide(:title, 'New Area')

= render 'layouts/flash' if !flash.empty?

.container-fluid
  / Page Heading
  .row
    .col-lg-12
      %h1.page-header
        Area
        %small Creator

  = render 'form'
