- provide(:title, 'New Area')

.container-fluid
  / Page Heading
  .row
    .col-lg-12
      %h1.page-header
        Area
        %small Creator

  = render 'layouts/flash' if !flash.empty?

  = render 'form'
