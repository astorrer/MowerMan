- provide(:title, 'New Egg Timer')

.container-fluid
  / Page Heading
  .row
    .col-lg-12
      %h1.page-header
        Egg Timer
        %small Creator

  = render 'layouts/flash' if !flash.empty?

  = render 'form'
