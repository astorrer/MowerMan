#warningModal.modal{"aria-hidden" => "true", "aria-labelledby" => "warningModal", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} &times;
          %span.sr-only Close
        %h4.modal-title Warning Task Details | Most Recently Updated
      .modal-body
        .table-responsive
          %table.table.table-hover
            %thead
              %tr
                %th Area
                %th Equipment
                %th Part
                %th Actions
            %tbody
              - @page_warning.each do |task|
                %tr
                  %td= task.area
                  %td= task.equipment
                  %td= task.part
                  %td= link_to 'VIEW', plantask_path(task), class: 'btn btn-default'
      .modal-footer
        = link_to "VIEW ALL #{@warning}", view_warnings_path, class: 'btn btn-warning'
        %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} CLOSE