- provide(:title, 'New Work Order')

.container-fluid
  / Page Heading
  .row
    .col-lg-12
      %h1.page-header
        Work Order
        %small Creator

  = render 'layouts/flash' if !flash.empty?

  = render 'form'