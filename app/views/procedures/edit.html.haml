- provide(:title, 'Edit Procedure')

= render 'layouts/flash' if !flash.empty?

.container-fluid
  / Page Heading
  .row
    .col-lg-12
      %h1.page-header
        Procedure
        %small Editor

  = render 'layouts/flash' if !flash.empty?

  = render 'form'