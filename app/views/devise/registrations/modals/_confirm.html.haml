#confirmModal.modal{"aria-hidden" => "true", "aria-labelledby" => "alertModal", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} &times;
          %span.sr-only Close
        %h4.modal-title Account Closure Confirmation
      .modal-body
        %h4 If you delete your account, your personal data can never be recovered. Are you sure this is what you want?
      .modal-footer
        = button_to "Yes, Delete my Account", registration_path(resource_name), method: :delete, class: 'btn btn-danger'